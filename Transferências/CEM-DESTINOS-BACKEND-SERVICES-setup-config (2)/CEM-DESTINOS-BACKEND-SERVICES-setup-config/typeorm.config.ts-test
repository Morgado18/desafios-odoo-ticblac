
import { DataSource } from 'typeorm';
import { Company } from './src/modules/transports/companies/entities/company.entity';

export const AppDataSource = new DataSource({
  type: 'postgres',
  host: 'localhost',
  port: 5432,
  username: 'postgres',
  password: '1234',
  database: 'cemdestinosservices',
  entities: [Company],
  synchronize: true,
  logging: true,
});
